นี่คือ **`spec.md` ฉบับสมบูรณ์ (Revision 2)** ที่เพิ่มหัวข้อ **"Mobile Optimization Strategy (iPhone XS Focus)"** เข้าไปโดยเฉพาะครับ

โจทย์เรื่องจอเล็ก (iPhone XS / SE หน้าจอกว้างประมาณ 375px) เป็นเรื่องปราบเซียนสำหรับเว็บภาษาไทย เพราะสระลอยและคำที่ยาวมักจะทำให้ Layout พัง เอกสารนี้จะเน้นเทคนิคการตัดคำ (Truncation) และการจัดวางเพื่อแก้ปัญหานี้ครับ

-----

# SPEC.MD: Size Guide Page (Mobile-First Edition)

**Project:** BOGUS - Luxury Jewelry Broker
**Target Device:** iPhone XS (375px width) & Up
**Key Constraint:** ป้องกัน Text ล้น, จัดการคำฉีก (Word Break), และควบคุมความสูงของการ์ดในจอเล็ก

-----

## 1\. Mobile Text Handling Strategy (กลยุทธ์จัดการข้อความบนจอเล็ก)

### **1.1 Typography Scaling (การย่อขยายตัวอักษร)**

*บนมือถือ เราต้องยอมลดขนาดฟอนต์ลงเพื่อรักษาพื้นที่*

| Element | Desktop Size | Mobile (XS) Size | Tailwind Class |
| :--- | :--- | :--- | :--- |
| **H1 (Hero)** | `48px` (3rem) | **`30px` (1.875rem)** | `text-3xl md:text-5xl` |
| **H2 (Card Title)** | `24px` (1.5rem) | **`20px` (1.25rem)** | `text-xl md:text-2xl` |
| **Body Text** | `16px` (1rem) | **`14px` (0.875rem)** | `text-sm md:text-base` |
| **Micro Text** | `14px` (0.875rem) | **`12px` (0.75rem)** | `text-xs md:text-sm` |

### **1.2 Word Breaking Rules (กฎการตัดคำภาษาไทย)**

ภาษาไทยมักจะมีปัญหาคำยาวทำให้ดันกล่องขยาย

  * **Global Rule:** ใช้ `break-words` เพื่อให้คำยาวๆ ตัดลงบรรทัดใหม่ได้
  * **Prevent Widow:** พยายามไม่ให้คำสั้นๆ ตกมาอยู่บรรทัดเดียว (ใช้ `text-balance` ใน CSS ใหม่ช่วยได้)

### **1.3 Truncation (การตัดคำ ...)**

เพื่อความสวยงามและการ์ดเท่ากัน เราจะซ่อนข้อความที่ยาวเกินไป

  * **Descriptions:** จำกัดไม่เกิน 3 บรรทัด (`line-clamp-3`)
  * **Card Titles:** จำกัด 1 บรรทัด ถ้าเกินให้ขึ้น ... (`truncate`)

-----

## 2\. Updated Component Specs (ลงดีเทลแต่ละส่วน)

### **Section 1: Hero Section (Mobile Optimization)**

  * **Layout:**
      * ความกว้าง `w-full`
      * Padding ด้านข้าง `px-4` (สำคัญมากสำหรับจอ 375px ห้ามใช้เยอะกว่านี้)
  * **Text Constraint:**
      * **H1:** อนุญาตให้ขึ้นบรรทัดใหม่ได้ 2 บรรทัด (ใช้ `leading-tight` ลดระยะห่างบรรทัด)
      * **Sub-headline:** จำกัดความกว้าง `max-w-[280px]` บนมือถือ เพื่อบีบให้ข้อความไม่ตกขอบจอ

### **Section 2: Recommended Card (The 3-Step Flow)**

*จุดปราบเซียน: ขั้นตอน 1-2-3 เรียงแนวนอนจะพังบนจอ XS*

  * **Layout Strategy (Responsive Switch):**
      * **Desktop:** แนวนอน (Horizontal Row)
      * **Mobile (XS):** **เปลี่ยนเป็นแนวตั้ง (Vertical Stack)** หรือ **Grid 2 แถว**
  * **Mobile Implementation:**
    ```tsx
    <div className="flex flex-col gap-6 md:flex-row md:justify-between relative">
      {/* บนมือถือเรียงลงมา เพื่อให้ข้อความ "ทักไลน์แจ้งแอดมิน" ไม่ถูกบีบจนอ่านไม่ออก */}
      ...
    </div>
    ```

### **Section 3: Size Chart (Table Handling)**

*ตารางจะล้นจอ XS แน่นอน 100% ต้องแก้ด้วยการเลื่อน*

  * **Wrapper:** ต้องมี `div` หุ้มตารางไว้
  * **CSS Class:** `overflow-x-auto` (เพื่อให้เลื่อนซ้ายขวาได้เฉพาะตาราง)
  * **Scroll Hint:** อาจใส่เงาหรือลูกศรบอกว่า "เลื่อนเพื่อดูต่อ"
  * **Sticky Column:** (Optional) ตรึงคอลัมน์แรก (mm) ไว้ แล้วให้คอลัมน์อื่นเลื่อน

### **Section 4: Expert Tips (Card Consistency)**

  * **Problem:** เนื้อหา Tips ยาวไม่เท่ากัน ทำให้การ์ดสูงต่ำไม่เท่ากัน ดูไม่แพง
  * **Solution:**
      * Fix ความสูงขั้นต่ำ `min-h-[120px]`
      * ใช้ `line-clamp-4` สำหรับเนื้อหา เพื่อตัดจบถ้าเขียนยาวเกินไป
      * ใส่ปุ่ม "Read more" เล็กๆ ถ้าเนื้อหายาวจริงๆ (แต่แนะนำให้เขียนสั้นดีกว่า)

-----

## 3\. Tailwind Implementation (Code Snippets for Dev)

โค้ดชุดนี้ปรับจูนมาเพื่อจอ 375px โดยเฉพาะ

```tsx
// 1. Hero Text (Responsive Text)
<h1 className="font-serif text-[#0B1021] text-3xl md:text-5xl leading-tight text-center px-2">
  ค้นพบขนาดที่ใช่... <br className="hidden md:block"/> เพื่อสัมผัสที่ลงตัว
</h1>

// 2. Recommended Card Description (Line Clamp)
<p className="font-light text-slate-500 text-sm md:text-base line-clamp-3 md:line-clamp-none mb-6">
  มั่นใจ 100% ด้วยอุปกรณ์มาตรฐานที่เราส่งให้คุณทดลองสวมใส่จริงถึงหน้าบ้าน 
  ฟรีไม่มีค่าใช้จ่ายแอบแฝง
</p>

// 3. Step Flow (Mobile Vertical / Desktop Horizontal)
<div className="flex flex-col md:flex-row justify-between items-center md:items-start gap-4 md:gap-0 relative">
   {/* Step Item */}
   <div className="flex flex-row md:flex-col items-center gap-3 md:gap-0 w-full md:w-1/3 z-10">
      <div className="w-10 h-10 ... shrink-0">💬</div> {/* shrink-0 ป้องกันไอคอนบี้ */}
      <p className="text-xs font-medium text-slate-800 text-left md:text-center break-words">
        แจ้งแอดมิน
      </p>
   </div>
   {/* ... Steps อื่นๆ ... */}
</div>

// 4. Table Wrapper (Horizontal Scroll)
<div className="w-full overflow-x-auto pb-2"> {/* pb-2 เพื่อให้ Scrollbar ไม่ทับเนื้อหา */}
  <table className="w-full min-w-[350px]"> {/* min-w บังคับให้ตารางกว้างอย่างน้อย 350px เพื่อไม่ให้บีบ */}
     ...
  </table>
</div>
```

-----

## 4\. Todo List: Mobile Optimization Check

  - [ ] **Viewport Meta:** เช็คว่าใน `<head>` มี `<meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">` หรือไม่ (ป้องกัน user zoom จนเว็บพัง)
  - [ ] **Check H1:** ลองเปิดในจอจำลอง iPhone SE/XS ว่าหัวข้อตัดบรรทัดสวยไหม (ไม่ตัดกลางคำ)
  - [ ] **Check Table:** ลองปัดตารางไซส์ซ้าย-ขวาบนมือถือ ต้องลื่นไหล
  - [ ] **Check Button:** ปุ่ม CTA ต้องเต็มความกว้าง (`w-full`) บนมือถือ เพื่อให้กดง่าย (Thumb Zone)
  - [ ] **Check Padding:** ขอบซ้ายขวาของหน้าเว็บต้องมี `px-4` หรือ `px-6` เสมอ เนื้อหาห้ามชิดขอบจอเด็ดขาด

การเพิ่ม Spec ส่วนนี้เข้าไป จะทำให้หน้า Size Guide ของคุณโบ๊ท **"รอดตาย"** ในทุกอุปกรณ์ ไม่ว่าลูกค้าจะใช้ iPhone รุ่นเล็กหรือรุ่น Pro Max ก็จะได้รับประสบการณ์ Luxury เหมือนกันครับ\!